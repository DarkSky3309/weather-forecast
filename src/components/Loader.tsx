import React from 'react';

const Loader = () => {
    return (
        <div className={"min-w-full"} style={{minHeight: 900, height: "100%"}}>
            
        </div>
    );
};

export default Loader;